<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springBootDemo.dao.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.springBootDemo.model.User">
        <id column="id" property="id"/>
        <result column="login_name" property="loginName"/>
        <result column="password" property="password"/>
        <result column="user_name" property="username"/>
    </resultMap>

    <sql id="BaseList">
        id, loging_name, password, user_name
    </sql>
    <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select <include refid="BaseList"/> from sys_user where id = #{0}
    </select>

    <select id="selectByName" resultMap="BaseResultMap" parameterType="java.lang.String">
        select <include refid="BaseList"/> from sys_user where login_name = #{0}
    </select>
</mapper>